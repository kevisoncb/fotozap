# Testing

```
npm run test        # unitários (Vitest)
npm run test:e2e    # placeholder até Postgres/Redis/Docker
npm run typecheck
npm run lint
```

Obrigatórios nas fases seguintes: concorrência de webhook de pagamento, webhook duplicado, restart de worker, eventos de IA fora de ordem.
